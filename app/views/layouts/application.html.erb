<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <link rel="apple-touch-icon" sizes="76x76" href="<%= image_path('apple-icon.png') %>">
  <link rel="icon" type="image/png" href="<%= image_path('favicon.ico') %>">
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
  <title>Light Bootstrap Dashboard - Free Bootstrap 4 Admin Dashboard by Creative Tim</title>
  <%= csrf_meta_tags %>
  <meta content='width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0, shrink-to-fit=no' name='viewport' />
  <!--     Fonts     -->
  <link href="https://fonts.googleapis.com/css?family=Montserrat:400,700,200" rel="stylesheet" />
  <!-- CSS Files -->

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
  <div class="wrapper">
    <%if user_signed_in?%>
      <%= render partial: "layouts/sidebar/left_side_bar" %>
    <% end %>
    <div class="main-panel <%= user_signed_in? ? '' : 'width-100'%>">
      <!-- Navbar -->
      <%= render partial: "layouts/header/header"%>
      <!-- End Navbar -->
      <div class="<%= user_signed_in? ? 'content' : ''%>">
        <%= yield %>
      </div>
      <%= render partial: "layouts/footer/footer"%>
    </div>
  </div>
</body>
</html>