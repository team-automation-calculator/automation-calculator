<div class="top_navigation">
  <div class="container">
    <div class="row">
      <div class="col-sm-4">
        <h3>Automation Calculator</h3>
      </div>

      <div class="col-sm-4 col-sm-offset-4">
          <span class="login_toggle">
        <% if user_signed_in? %>
            <%= link_to('Logout', destroy_user_session_path, method: :delete) %>
        <% else %>
            <%= link_to('Login', new_user_session_path)  %>
        <% end %>
          </span>
      </div>
    </div>
  </div>
</div>